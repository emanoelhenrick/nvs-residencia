spring.application.name=ams
server.port=${SERVER_PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/nvs-db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:docker}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:docker}

jwt.secret=${JWT_SECRET:local-development-only-change-me}
